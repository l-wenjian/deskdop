const state = {
    zIndex: 1,
}

const mutations = {
    // 设置属性
    setZIndex: function(state) {
        state.zIndex += 1
    },
}

const actions = {

}

export default {
    state,
    mutations,
    actions
}
